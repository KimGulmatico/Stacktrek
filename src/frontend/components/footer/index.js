import React from 'react';
import { Layout } from 'antd';
const { Footer } = Layout;

const PageFooter = (props) => {
  return (
    <Footer style={{ position: 'fixed', width: '100%' }}>
	    THIS IS THE ASDFASDF
    </Footer>
  )
}

export default PageFooter;