import React from 'react'

const title = ({ title, user }) => (
  <div className="holder padded-holder">
    <div className="title">Connections</div>
    <div>Obed has connected with these people.</div>
  </div>
)

export default title
